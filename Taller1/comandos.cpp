#include "comandos.h"

void create(int num, char x){

    if((num < 0) || (x != 88 || x != 120)){

        printf("Por favor ingresa un parametro correcto");
    }
    else{

        int *num = new int;
        char *x = new char;
        //deberia crear uno solo, dependiendo el parametro que se le pase
    }


}
