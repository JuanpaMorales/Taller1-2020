#include "comandos.h"

void create(int num, char x){

    if((num < 0) || (x != 'x' x != 'X')){

        printf("Por favor ingresa un parametro correcto");
    }
    else{

        if(num >= 0)
            int num = new int;
        else
            if(x == 'x' || x == 'X')
            char x = new char;
    }


}
